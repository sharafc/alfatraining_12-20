import React, { ReactElement } from "react";

export default function Header(): ReactElement {
    return (
        <footer className="ui footer segment">
            <div className="ui center aligned container">Start Exploring</div>
        </footer>
    );
}
